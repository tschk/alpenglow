{
  "scripts": {
    "dev": "cd site && bun run dev",
    "build:v86": "V86_SSH=1 sh scripts/build-v86-initramfs.sh",
    "build:v86:local": "sh scripts/build-v86-initramfs.sh",
    "test:site": "sh scripts/test-site-v86-preload.sh && cd site && bun test",
    "build:site": "sh scripts/write-site-version.sh && cd site && bun run typecheck && bun run build",
    "build": "V86_SSH=1 sh scripts/build-v86-initramfs.sh && bun run build:site",
    "preview": "cd site && bun run start",
    "deploy": "bun run build && wrangler pages deploy site/dist --project-name alpenglow"
  },
  "devDependencies": {
    "wrangler": "4.107.0"
  }
}
