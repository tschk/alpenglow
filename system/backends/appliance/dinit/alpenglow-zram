# Compressed swap (zram) — memory pressure relief for diskless mode
type = scripted
command = /bin/toybox sh -c "/usr/local/bin/alpenglow-zramctl-zig || true"
depends-on = alpenglow-kernel-policy
restart = no
