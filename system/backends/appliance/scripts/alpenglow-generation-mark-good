#!/bin/sh
# Scaffold hook named by rootfs-layout.json. Does not perform an update.
set -eu

STATE_DIR="/var/lib/alpenglow/system"
mkdir -p "${STATE_DIR}"
printf '{"status":"good","marked_at":"%s"}\n' "$(date -u +%Y-%m-%dT%H:%M:%SZ 2>/dev/null || date)" \
  > "${STATE_DIR}/update-state.json"
exit 0
